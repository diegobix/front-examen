export type State = {
  name: string;
  email: string;
  id: string;
};

export type VideoType = {
  title: string;
  thumbnail: string;
  description: string;
  duration: number;
  youtubeid: string;
  date: string;
  id: string;
  fav: boolean;
};
